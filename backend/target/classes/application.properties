spring.application.name=erp-automacao

# Datasource configuration using environment variables
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?user=postgres.mhxycuagyraepaxmpekg&password=fagocitose!&prepareThreshold=0
spring.datasource.username=postgres.mhxycuagyraepaxmpekg
spring.datasource.password=fagocitose!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
# Disable DDL-auto to prevent modifying the existing Supabase tables
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Show SQL for debugging (optional, can be disabled in production)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Server port
server.port=8081
